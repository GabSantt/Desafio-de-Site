git add . - pega o que fiz de diferente e coloca na 'caixa'

git status- mostra o q modifiquei

git commit -m (comando para deixar mensagem de cmomit)  'mensagem'

git push- joga no git

git pull - "pegar" as mudanças
